
import {Box} from "@chakra-ui/react";
import {useReactTable} from "@tanstack/react-table";

const dataTable= () => {
    const table = useReactTable({
        data:    
                                          /* data is array in video*/
    })

    return<Box></Box>
    
};

export default dataTable;